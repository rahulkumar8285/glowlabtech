services:
  growthtechsys-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: growthtechsys-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
