services:
  glowlab-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: glowlab-tech-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
